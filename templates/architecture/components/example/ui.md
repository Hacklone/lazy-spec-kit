# UI: Example

> Document the user-facing entry points of this application.
> Adapt the sections below to your app type — delete sections that don't apply.
> This file helps agents understand the app's user-facing structure
> when adding features or modifying interactions.
>
> **Web apps** → pages/routes. **Mobile** → screens. **Desktop** → windows/panels.
> **CLI** → commands/subcommands.

<!-- === Choose the section that matches your app type === -->

<!-- WEB / MOBILE / DESKTOP — Page or screen breakdown:

## Screen: Dashboard Home

- **Route / Entry:** `/` (web) or `DashboardScreen` (mobile)
- **Purpose:** Overview metrics, activity feed, quick actions
- **Key components:** MetricsGrid, ActivityFeed, QuickActions
- **Data sources:** analytics API, user API
- **Auth required:** Yes

## Screen: Settings

- **Route / Entry:** `/settings` or `SettingsScreen`
- **Purpose:** User profile, preferences, notification settings
- **Key components:** SettingsForm, ToggleGroup
- **Data sources:** auth API (user profile)
- **Auth required:** Yes
-->

<!-- CLI — Command breakdown:

## Command: build

- **Usage:** `mytool build [--target <env>] [--watch]`
- **Purpose:** Compile project for specified environment
- **Flags:** `--target` (prod|dev), `--watch` (rebuild on change)
- **Output:** Build artifacts in `dist/`
- **Side effects:** None

## Command: deploy

- **Usage:** `mytool deploy <stage> [--region <region>]`
- **Purpose:** Deploy built artifacts to target stage
- **Flags:** `--region` (aws region), `--dry-run` (preview only)
- **Output:** Deployment URL and status
- **Side effects:** Modifies remote infrastructure
-->
