# Library: Example

> Replace "Example" with your library name. Name this file `overview-<library-name>.md`.
> This file should be self-contained — an agent reading ONLY this file
> should understand the library well enough to use it correctly.

## Purpose

<!-- What does this library provide? Why does it exist? 2-3 sentences. -->

## Ownership

<!-- Team or person responsible for this library. -->

## Public API

<!-- Key exports, functions, or classes this library provides.

| Export | Purpose |
|--------|---------|
| `validate(schema, data)` | Validate input against a schema |
| `sanitize(input)` | Sanitize user input |
| `formatError(error)` | Format errors to standard shape |
-->

## Consumers

<!-- Which services and apps use this library?

- auth-service
- billing-service
- dashboard (app)
-->

## Constraints

<!-- Rules for using or extending this library.

- Must remain framework-agnostic (no React/Express dependencies)
- All exports must have TypeScript types
- Breaking changes require a major version bump
-->
